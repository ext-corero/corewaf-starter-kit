# Customer-supplied configuration (never committed)
config.ini

# Rendered runtime artifacts (output of the templates stage)
runtime/
.runtime/

# Local Docker / orchestrator state
.discover/
discovery.json
hwinfo/

# Compose-time substitutions (DOCKER_GID etc.) — generated per host
.env

# Editor / OS detritus
.DS_Store
*.swp
*.swo
*~
.idea/
.vscode/
