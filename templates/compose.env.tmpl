# Rendered by `orchestrator templates render` — do not edit by hand.
# Source: templates/compose.env.tmpl
# Inputs: config.ini + discovery output (see scripts/discover.sh).

# Identity
ORG_SCOPE_ID={{ .Env.ORG_SCOPE_ID }}
INSTANCE_COMMENT={{ .Env.INSTANCE_COMMENT }}
INSTANCE_NAME={{ .Env.INSTANCE_NAME }}
HOSTNAME={{ .Env.HOSTNAME }}
MACHINE_ID={{ .Env.MACHINE_ID }}
HARDWARE_FINGERPRINT={{ .Env.HARDWARE_FINGERPRINT }}

# Backend (one base URL — telemetry and api-server route under it)
OBSERVABILITY_BASE_URL={{ .Env.OBSERVABILITY_BASE_URL }}
API_GATEWAY_URL={{ .Env.API_GATEWAY_URL }}
LOKI_URL={{ .Env.LOKI_URL }}
MIMIR_URL={{ .Env.MIMIR_URL }}

# Caddy
CADDY_ADMIN_PORT={{ .Env.CADDY_ADMIN_PORT }}
